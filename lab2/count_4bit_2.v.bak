module count_4bit_2(
	input Ena,CLK,Reset,
	output Q
);
	always @(posedge CLK or negedge Reset) begin
		if (!Reset) begin
         Q <= 1'b0; 
		end else begin
			if(CLK && Ena) begin
				Q <= Q + 1;
			end
		end
endmodule
	
